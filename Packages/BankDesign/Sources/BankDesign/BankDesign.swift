// BankDesign — BankSmartAI feature package.
// Deliberately empty: implementation arrives via the Feature Engineer
// Agent, against contracts signed at Seam 2. See architecture/PLAN.md.

public enum BankDesignPackage {
    /// Placeholder so the empty package compiles. Removed by the first
    /// real implementation.
    public static let placeholder = true
}
